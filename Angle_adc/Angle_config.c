#include "Angle_config.h"

ANGLE_CONFIG angle_config = {
    .adc_port = &hadc1,  // Initialize ADC handle to point to hadc1

    .index = 0,          // Initialize index to 0
    .size = 1            // Initialize size to 1

};